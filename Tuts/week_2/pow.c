#include <stdio.h>

int 
pow(int x, unsigned int n);

int
main(void)
{
	return 0;
}

int 
pow(int x, unsigned int n)
{
	if(n == 1)	return x;
	
	return x * (1.....n)
}

res = res * x;

product = 1
if n == 1, product = 1
return 1*funcall(1+1)

int 
func_1(int max_bound, int n)
{
	if(max_bound == n)	return = max_bound;	
	return 1*func_1(max_bound, )
}
